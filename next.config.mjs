/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['i.ibb.co'], // Add all external image domains you use here
    },
    // ... any other config you may have
  };
  
  export default nextConfig;
  